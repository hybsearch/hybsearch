---
version: 2

x-config:
  - &name "hybsearch/hybsearch"
  - &defaults
    docker:
      - image: circleci/node:8-stretch
    environment: &default-environment
      LOCAL_NAME: *name
      DEST_NAME: docker.io/hybsearch/hybsearch
  - &caches
    - &cache-load-docker
      keys:
        - docker--{{ arch }}-{{ .Branch }}-{{ .Revision }}
        - docker--{{ arch }}-{{ .Branch }}
        - docker--{{ arch }}
    - &cache-save-docker
      key: docker--{{ arch }}-{{ .Branch }}-{{ .Revision }}
      paths:
        - /tmp/image.tar.gz
    - &cmd-docker-load
      name: 'Load from cache if possible'
      command: |
        if test -r /tmp/image.tar.gz; then
          echo "Loading from /tmp/image.tar.gz"
          docker load -qi /tmp/image.tar.gz
        else
          echo "missing /tmp/image.tar.gz; failing the build"
          exit 1
        fi
    - &cmd-docker-save
      name: 'Dump image to cachable .tar.gz file'
      command: docker save "$LOCAL_NAME:$CIRCLE_SHA1" | pigz -9c > /tmp/image.tar.gz

workflows:
  version: 2
  on_commit:
    jobs:
      - build
      - lint
      - test-app
      - test-server
      - test-pipeline-mrbayes:
          requires: [build]
      - test-pipeline-beast:
          requires: [build]
      - deploy-docker:
          requires: [lint, test-server, test-pipeline-beast, test-pipeline-mrbayes, test-app]
          context: docker-hub
      - build-app-windows:
          requires: [lint, test-app]
      - build-app-mac:
          requires: [lint, test-app]
      - build-app-linux:
          requires: [lint, test-app]

  on_tag:
    jobs:
      - build:
          filters: &filters
            branches:
              ignore: /.*/
            tags:
              only: /^v.*/
      - lint:
          filters: *filters
      - test-app:
          filters: *filters
      - test-server:
          filters: *filters
      - test-pipeline-mrbayes:
          requires: [build]
          filters: *filters
      - test-pipeline-beast:
          requires: [build]
          filters: *filters
      - deploy-docker:
          requires: [lint, test-server, test-pipeline-beast, test-pipeline-mrbayes, test-app]
          filters: *filters
          context: docker-hub
      - build-app-windows:
          requires: [lint, test-server, test-pipeline-beast, test-pipeline-mrbayes, test-app]
          filters: *filters
      - build-app-mac:
          requires: [lint, test-server, test-pipeline-beast, test-pipeline-mrbayes, test-app]
          filters: *filters
      - build-app-linux:
          requires: [lint, test-server, test-pipeline-beast, test-pipeline-mrbayes, test-app]
          filters: *filters

  cron_weekly:
    jobs:
      - build
      - test-app
      - test-server
      - test-pipeline-mrbayes:
          requires: [build]
      - test-pipeline-beast:
          requires: [build]
    triggers:
      - schedule:
          # only run Monday morning at 12:00am
          cron: 0 0 * * 1
          filters:
            branches:
              only: [master]

jobs:
  # build the docker image
  build:
    <<: *defaults
    steps:
      - checkout
      - setup_remote_docker
      - run:
          name: List docker images
          command: docker images -a
      - run:
          name: Build docker image
          command: docker build --cache-from="$(docker images -a -q)" -t "$LOCAL_NAME:$CIRCLE_SHA1" ./server
      - run: sudo apt install -y pigz
      - run: *cmd-docker-save
      - save_cache: *cache-save-docker

  # run sanity checks
  lint:
    <<: *defaults
    steps:
      - checkout
      - run: npm install
      - run: npm run lint
      - run: npm run pretty-check

  # check that the whole pipeline runs at all
  test-pipeline-mrbayes:
    <<: *defaults
    steps:
      - checkout
      - setup_remote_docker
      - restore_cache: *cache-load-docker
      - run: *cmd-docker-load
      - run: docker run -i "$LOCAL_NAME:$CIRCLE_SHA1" /hybsearch/pipeline/hyb-pipeline search - < data/emydura-short.gb

  test-pipeline-beast:
    <<: *defaults
    steps:
      - checkout
      - setup_remote_docker
      - restore_cache: *cache-load-docker
      - run: *cmd-docker-load
      - run: docker run -i "$LOCAL_NAME:$CIRCLE_SHA1" /hybsearch/pipeline/hyb-pipeline beast - < data/emydura-short.gb

  # test the server-side js code
  test-server:
    <<: *defaults
    steps:
      - checkout
      - run: npm install
      - run: cd server && npm install
      - run: cd server && npm test

  # deploy the docker images to Docker Hub
  deploy-docker:
    <<: *defaults
    steps:
      - checkout
      - setup_remote_docker
      - restore_cache: *cache-load-docker
      - run: *cmd-docker-load
      - run: docker login -u "$DOCKER_USERNAME" -p "$DOCKER_PASSWORD"
      - run:
          name: Push to Docker Hub
          command: |
            image_id="$(docker images -q "$LOCAL_NAME:$CIRCLE_SHA1")"
            echo "image_id: $image_id"

            if [ -z $image_id ]; then
              echo "could not find docker image to load; exiting"
              exit 1
            fi

            echo "CIRCLE_BRANCH: $CIRCLE_BRANCH" "CIRCLE_TAG: $CIRCLE_TAG"
            if [[ $CIRCLE_BRANCH = master ]]; then
              docker_tag="$DEST_NAME:HEAD"
            elif [[ $CIRCLE_TAG ]]; then
              docker_tag="$DEST_NAME:$CIRCLE_TAG"
            elif [[ $CIRCLE_BRANCH ]]; then
              docker_tag="$DEST_NAME:$CIRCLE_BRANCH"
            fi
            echo "docker_tag: $docker_tag"

            docker tag "$image_id" "$docker_tag"
            docker push "$docker_tag"

            if [[ $CIRCLE_TAG ]]; then
              docker tag "$image_id" "$DEST_NAME:latest"
              docker push "$DEST_NAME:latest"
            fi

  # deploy the compiled electron binaries to GitHub Releases
  build-app:
    <<: *defaults
    steps:
      - checkout
      - run:
          name: Update the electron package version number
          command: npm run propagate-version
      - run: cd electron && npm install
      - run: cd electron && npm run build
      # - run: cd electron && npm run gh-pages
