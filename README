to install:

- os x:
	- install homebrew: <https://brew.sh>
	- install node: brew install node
	- install openmpi: brew install openmpi --with-mpi-thread-multiple

- windows:
	- install chocolaty: <https://chocolatey.org>
	- install git, python, and node: choco install git python2 nodejs.install

- install local node modules: npm install


pipeline:

	cat data/emydura-short.gb | ./bin/genbank-fasta.js - | ./bin/clustal-o.js - | ./bin/fasta-to-nexus.js - | ./bin/mrbayes.js - | ./bin/consensus-newick.js - | ./bin/newick-json.js - | ./ent.js -

to convert files:

	./vendor/seqmagick-cli convert --input-format fasta --output-format nexus --alphabet dna <input> <output>
